export interface IContact {
  name: string;
  library: string;
  icon: string;
  description: string;
}

export interface IContactForm {
  name: string;
  email: string;
  subject: string;
  message: string;
}
